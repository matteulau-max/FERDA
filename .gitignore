node_modules
dist
# `vite build --outDir dist-something`, used when two builds need serving side
# by side (comparing a change against the build before it, for instance).
dist-*
.env.local
.env.*.local
*.local
.env
